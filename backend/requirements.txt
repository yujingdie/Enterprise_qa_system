# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# 数据库
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2

# 配置
pyyaml==6.0.2
pydantic==2.9.2
pydantic-settings==2.5.2
marshmallow<4             # environs(pymilvus依赖)需要 __version_info__

# LLM (LangChain + OpenAI 兼容接口)
langchain==0.3.17
langchain-openai==0.3.5

# Embedding / LangChain (千问 OpenAI 兼容接口)
openai>=1.58.1

# Milvus
pymilvus==2.4.4

# Embedding (本地模型) — 可选，默认用千问 API
# sentence-transformers==3.1.1

# Reranker — Cross-encoder 精排
sentence-transformers==3.1.1

# 文档解析
PyMuPDF==1.24.10
python-docx==1.1.2
python-pptx==1.0.2

# OCR — 扫描件PDF文字识别
# pytesseract 在 Dockerfile 中安装（需要宿主机 apt-get install tesseract-ocr）
Pillow>=10.0.0

# 测试
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# 工具
aiofiles==24.1.0
